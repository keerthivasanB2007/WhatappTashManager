import React from 'react'; import { useAppState } from '../../hooks/useAppState'; import { useTasks } from '../../hooks/useTasks'; import { getTaskCategory } from '../../utils/taskUtils';
const icons={TODAY:'☀',ALL:'▦',UPCOMING:'↗',OVERDUE:'!',HIGH_PRIORITY:'◆',COMPLETED:'✓',CALENDAR:'□',SENDERS:'♙'};
export default function Sidebar(){const {currentView,setCurrentView,filter,setFilter,sidebarCollapsed,setSidebarCollapsed}=useAppState();const {globalTasks}=useTasks();const pending=globalTasks.filter(t=>t.status!=='COMPLETED');const counts={ALL:globalTasks.length,UPCOMING:pending.filter(t=>['TOMORROW','THIS_WEEK','LATER'].includes(getTaskCategory(t))).length,OVERDUE:pending.filter(t=>getTaskCategory(t)==='OVERDUE').length,HIGH_PRIORITY:pending.filter(t=>t.priority==='HIGH').length,COMPLETED:globalTasks.filter(t=>t.status==='COMPLETED').length};const nav=[['TODAY','Today'],['ALL','All tasks'],['UPCOMING','Upcoming'],['OVERDUE','Overdue'],['HIGH_PRIORITY','High priority'],['COMPLETED','Completed']];const go=key=>{setCurrentView('TASKS');setFilter(key)};return <aside className="sidebar"><div className="brand"><span className="brand-mark">✓</span>{!sidebarCollapsed&&<span>Taskflow</span>}<button className="collapse-btn" onClick={()=>setSidebarCollapsed(!sidebarCollapsed)} aria-label="Toggle sidebar">‹</button></div><div className="nav-label">{!sidebarCollapsed&&'WORKSPACE'}</div><nav>{nav.map(([key,label])=><button key={key} title={label} className={`nav-item ${currentView==='TASKS'&&filter===key?'active':''}`} onClick={()=>go(key)}><i>{icons[key]}</i>{!sidebarCollapsed&&<span>{label}</span>}{!sidebarCollapsed&&counts[key]!==undefined&&<b className={key==='OVERDUE'?'danger':''}>{counts[key]}</b>}</button>)}</nav><div className="nav-label">{!sidebarCollapsed&&'ORGANIZE'}</div><nav><button title="Calendar" className={`nav-item ${currentView==='CALENDAR'?'active':''}`} onClick={()=>setCurrentView('CALENDAR')}><i>{icons.CALENDAR}</i>{!sidebarCollapsed&&<span>Calendar</span>}</button><button title="Sender groups" className={`nav-item ${filter==='SENDERS'?'active':''}`} onClick={()=>go('SENDERS')}><i>{icons.SENDERS}</i>{!sidebarCollapsed&&<span>Sender groups</span>}</button></nav><div className="sidebar-bottom"><span className="online-dot" />{!sidebarCollapsed&&'System online'}</div></aside>}
