import React, { useEffect, useRef } from 'react';
import { useAppState } from '../../hooks/useAppState';
import { useAuth } from '../../hooks/useAuth';
export default function TopBar() { const { searchQuery, setSearchQuery, setRailHidden, railHidden } = useAppState(); const { logout } = useAuth(); const input = useRef(); useEffect(() => { const listener=e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();input.current?.focus();}};window.addEventListener('keydown',listener);return()=>window.removeEventListener('keydown',listener);},[]); return <header className="global-topbar"><div className="crumb"><span>Workspace</span><strong>Task dashboard</strong></div><label className="global-search"><span>⌕</span><input ref={input} value={searchQuery} onChange={e=>setSearchQuery(e.target.value)} placeholder="Search tasks, messages, or senders"/><kbd>Ctrl K</kbd></label><div className="top-actions"><button title="Toggle insights" className="top-action" onClick={()=>setRailHidden(!railHidden)}>▤</button><button className="avatar" onClick={logout} title="Log out">K</button></div></header>; }
